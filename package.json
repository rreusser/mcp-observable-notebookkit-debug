{
  "name": "@rreusser/mcp-observable-notebookkit-debug",
  "version": "0.0.0",
  "type": "module",
  "description": "MCP server for debugging Observable Notebook Kit notebooks",
  "main": "src/vite/plugin.js",
  "bin": {
    "mcp-notebookkit-debug": "./bin/mcp-notebookkit-debug.js"
  },
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "peerDependencies": {
    "@observablehq/notebook-kit": "^1.5.0"
  }
}
